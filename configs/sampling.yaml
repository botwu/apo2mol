model:
  checkpoint: ./apo2mol_dataset/apo2mol_checkpoint.ckpt

sample:
  seed: 2021
  num_samples: 5
  num_steps: 1000
  pos_only: False
  center_pos_mode: protein
  init_center_mode: holo # [holo, apo]
  sample_num_atoms: ref # [prior, range, ref]
